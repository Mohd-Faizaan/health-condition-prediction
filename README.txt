Vercel-ready files for the health-condition-prediction project.

Before using these files, rename your current Streamlit app.py to streamlit_app.py if you want to keep it.
Copy app.py, backend.py, requirements.txt and vercel.json into the project root.
The large model is downloaded at runtime from the public GitHub LFS media URL, so it is not uploaded as a Vercel source file.
